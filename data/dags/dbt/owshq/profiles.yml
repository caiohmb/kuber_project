minio:
  target: dev
  outputs:
    dev:
      type: trino
      threads: 1
      catalog: minio
      schema: landing
      host: trino.local
      port: 80
      user: trino

iceberg:
  target: dev
  outputs:
    dev:
      type: trino
      threads: 1
      catalog: iceberg
      schema: stage
      host: trino.local
      port: 80
      user: trino
